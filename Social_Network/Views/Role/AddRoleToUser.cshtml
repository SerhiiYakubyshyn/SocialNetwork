@*@using Microsoft.AspNetCore.Identity;
@model LibrarySiteView.Models.RoleViewModel

@{
    ViewData["Title"] = "AddRoleToUser";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>AddRoleToUser @Model.UserEmail</h1>

<h4>RoleViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddRoleToUser" method="post">
            <input type="hidden" name="UserId" value="@Model.UserId" />
            <select asp-for="" asp-item="" name="role">

                @foreach (var r in (ViewData["AllRoles"] as List<IdentityRole>))
                {
                    <option value="@r.Name">
                        @r.Name
                    </option>
                }
            </select>

            <div class="form-group">
                <input type="submit" value="Add" class="btn btn-primary" />
            </div>

        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>


@section footer
{
}*@